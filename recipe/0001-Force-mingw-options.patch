From e0cd19edecec9edacb2f79b446fdfccd562be6f7 Mon Sep 17 00:00:00 2001
From: "Uwe L. Korn" <uwe.korn@quantco.com>
Date: Fri, 24 Jun 2022 12:57:43 +0200
Subject: [PATCH] Force mingw options

---
 setup.py | 22 +++++++---------------
 1 file changed, 7 insertions(+), 15 deletions(-)

diff --git a/setup.py b/setup.py
index 33aae115..29b91378 100644
--- a/setup.py
+++ b/setup.py
@@ -120,21 +120,13 @@ extra_compile_args = [
 
 if platform.platform().startswith('Win'):
     from Cython.Build.Inline import _get_build_extension
-    if _get_build_extension().compiler in (None, 'msvc'):
-        print("Warning: MSVC is not supported")
-        extra_compile_args = [
-            '/EHsc',
-            '-DBOOST_DATE_TIME_NO_LIB',
-            '/std:c++14',
-        ]
-    else:
-        # fix bug in MingW-W64
-        # use posix threads
-        extra_compile_args.extend([
-            "-D_hypot=hypot",
-            "-pthread",
-            "-fexceptions",
-            ])
+    # fix bug in MingW-W64
+    # use posix threads
+    extra_compile_args.extend([
+        "-D_hypot=hypot",
+        "-pthread",
+        "-fexceptions",
+        ])
 
 
 stanc_sources = [
-- 
2.35.3

